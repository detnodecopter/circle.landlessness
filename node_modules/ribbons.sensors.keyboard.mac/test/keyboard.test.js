Keyboard = require('../index');
var keyboard = new Keyboard();

console.info('start typing (ctrl+c to exit)');

keyboard.start();

keyboard
  .on('stroke', function(key) {})

setTimeout(function(){
  keyboard.stop();
}, 5000);