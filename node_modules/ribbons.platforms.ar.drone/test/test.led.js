var ARDrone = require('../index');

var arDrone = new ARDrone();

var arDroneRed = new arDrone.Pin('leftRedRightGreen');

arDroneRed.off();

// TODO should be a board.on('ready', ); 
setTimeout(function arDroneRedOn() {
  arDroneRed.on();
}, 2000);

setTimeout(function arDroneRedOff() {
  arDroneRed.off();
}, 4000);

setTimeout(function arDroneRedOn() {
  arDroneRed.on();
}, 6000);

setTimeout(function arDroneRedOff() {
  process.exit();
}, 8000);
